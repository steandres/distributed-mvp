services:

  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: characters
    ports:
      - "5432:5432"

  auth-service:
    image: estephanc/auth-service:1.0
    ports:
      - "3001:3001"
    environment:
      - JWT_SECRET=supersecret
    depends_on:
      - postgres
      - kafka

  backend-service:
    image: estephanc/backend-service:1.0
    ports:
      - "3002:3002"
    environment:
      - DB_HOST=postgres
      - DB_USER=user
      - DB_PASSWORD=password
      - DB_NAME=characters
    depends_on:
      - postgres
      - kafka

  frontend-service:
    image: estephanc/frontend-service:1.0
    ports:
      - "80:3000"
    depends_on:
      - auth-service
      - backend-service

  zookeeper:
    image: confluentinc/cp-zookeeper:7.5.0
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    ports:
      - "2181:2181"

  kafka:
    image: confluentinc/cp-kafka:7.5.0
    depends_on:
      - zookeeper
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      
      # 🔥 CLAVE PARA QUE FUNCIONE EN DOCKER
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
